<form action="/{{todo._id}}/edit" method="POST">
<input type="checkbox" name="isDone" {{#if todo.isDone}} checked {{/if}}>
<input type="text" name="name" value="{{todo.name}}">
<button type="submit">edit</button>
<a href="/todos/{{todo._id}}">Back</a>
</form>